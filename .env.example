# 复制成 .env 后填入。.env 已被 .gitignore 排除，不会进版本库。
#
# refresh_token 等价于你的 pixiv 账号密码：只在本机使用，
# 不要提交、不要发给任何人、不要上传到任何服务器。

PIXIV_REFRESH_TOKEN=

# 可选。没有代理直连不通时填，格式 host:port。
PIXIV_PROXY=

# 可选。影响作品标题与 tag 翻译的语言，默认 zh-CN。
PIXIV_LANG=zh-CN
